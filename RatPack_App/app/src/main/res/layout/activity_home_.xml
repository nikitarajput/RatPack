<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:id="@+id/logout_button"
        android:layout_width="137dp"
        android:layout_height="wrap_content"
        android:onClick="onLogout"
        android:text="@string/button_logout"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="16dp"
        android:layout_marginLeft="26dp"
        app:layout_constraintLeft_toLeftOf="parent" />

    <TextView
        android:id="@+id/login_status_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/logged_in"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1"
        android:layout_marginLeft="124dp"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_marginTop="127dp"
        app:layout_constraintTop_toBottomOf="@+id/logout_button" />

    <Button
        android:id="@+id/to_rat_sightings_button"
        android:layout_width="156dp"
        android:layout_height="46dp"
        android:layout_marginBottom="2dp"
        android:layout_marginLeft="44dp"
        android:text="@string/rat_sightings"
        android:onClick="toRatSightings"
        app:layout_constraintBottom_toBottomOf="@+id/logout_button"
        app:layout_constraintLeft_toRightOf="@+id/logout_button" />

    <Button
        android:id="@+id/to_rat_map"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Rat Map"
        android:onClick="toRatMap"
        app:layout_constraintTop_toBottomOf="@+id/logout_button" />

</android.support.constraint.ConstraintLayout>
